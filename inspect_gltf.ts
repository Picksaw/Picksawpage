import fs from 'fs';
// We'll write a small node script to load the glb files and count meshes
// Actually, we can just use three.js in node with a headless approach, but maybe it's easier to check the console logs in the browser.
// Let's add a script to main.tsx to log the mesh counts.
